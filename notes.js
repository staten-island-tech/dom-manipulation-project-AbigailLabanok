const button = document.getElementById("btn"); //you can use getElementById when selecting an element by its id
const text = document.querySelector("#text"); //you can use querySelector when selecting an element by its class
console.log(button);
